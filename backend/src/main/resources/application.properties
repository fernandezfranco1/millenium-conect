# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/millenium_conect?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=milleniumconectsecretkeyforsigningjwttokens2024taekwondomanagement
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Logging
logging.level.com.millenium=DEBUG
logging.level.org.springframework.security=DEBUG

# File Upload Configuration
upload.path=uploads/comprobantes
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
